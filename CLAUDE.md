# CLAUDE.md

This project keeps a single set of agent instructions in [AGENTS.md](AGENTS.md), which every
coding agent reads. This file exists only so Claude Code picks it up automatically.

**Do not add guidance here.** Edit `AGENTS.md` instead — anything written below will drift out
of sync with the agents that never read this file.

@AGENTS.md
